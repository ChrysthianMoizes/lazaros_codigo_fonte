version: '2'
services:
  postgresql:
    image: postgres:11.5
    # volumes:
    #     - ~/volumes/jhipster/srsservice/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=lazaros
      - POSTGRES_PASSWORD=lazaros
    ports:
      - 5432:5432
